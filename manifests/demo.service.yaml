apiVersion: v1
kind: Service
metadata:
  name: demo-app-svc
  namespace: demo-app
spec:
  type: NodePort
  ports:
    - nodePort: 65535
      targetPort: http-port
      port: 30999
  selector:
    app: demo-app